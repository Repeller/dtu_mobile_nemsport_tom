<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dtu.nemsport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nemsport">
        <activity
            android:name=".OpretProfil"
            android:exported="true" />
        <activity
            android:name=".GlemtKode"
            android:exported="true" />
        <activity
            android:name=".Login"
            android:exported="true" />
<!--        <activity-->
<!--            android:name=".Forside"-->
<!--            android:exported="true" />-->
        <activity
            android:name=".MainPage"
            android:exported="true" />
        <!-- this maybe gonna be buggy -->
        <activity android:name="com.example.bottomnavbar.MainActivity">
<!--            android:name=".OpretProfilAktivitet"-->
<!--            android:exported="true" />-->
        <!-- end of buggy -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>